---
id: TASK_0001
state: completed
phase: done
created_at: 2026-06-30T16:25:52.230Z
updated_at: 2026-06-30T16:34:12.813Z
title: Make the page at `apps/web/src/routes/accessibility.tsx` fully responsive across all viewport widths (mobile, tablet, desktop). The page should follow the same responsive patterns already established by the sibling `privacy.tsx` route — consistent hero layout, content card sizing, typography scaling, and horizontal overflow handling — so that the accessibility content renders cleanly on small screens through wide desktops with no horizontal scrolling, no text overflow, and appropriately scaled spacing and font sizes.
label: Make accessibility.tsx responsive like privacy.tsx
---

## raw prompt

make the following page responsive: @apps/web/src/routes/accessibility.tsx

## refined prompt

Now I have enough context. Let me analyze the accessibility page against similar pages in the project and produce the rewritten task description.

GOAL
Make the page at `apps/web/src/routes/accessibility.tsx` fully responsive across all viewport widths (mobile, tablet, desktop). The page should follow the same responsive patterns already established by the sibling `privacy.tsx` route — consistent hero layout, content card sizing, typography scaling, and horizontal overflow handling — so that the accessibility content renders cleanly on small screens through wide desktops with no horizontal scrolling, no text overflow, and appropriately scaled spacing and font sizes.

CONSTRAINTS
- Preserve every existing element, component import, route definition (`createFileRoute("/accessibility")`), and semantic structure (`<main>`, hero `<div>`, content card `<div>`) exactly as they are.
- Preserve all existing internal link `href` values (`/about/contact`, `/certifications/certification-resources/accommodations`) verbatim.
- Preserve the existing design token classes (`bg-pmi-surface-secondary`, `bg-pmi-surface-primary`, `text-pmi-text-primary`, `font-aeonik`, `--pmi-rounded-md`) — do not replace them with hardcoded colors or values.
- Preserve the inline `style` object on the content card (`borderRadius`, `marginTop` using CSS custom properties).
- Do not add or remove any textual content, headings, paragraphs, or links.
- Do not change the Breadcrumb component or its props.
- Match the responsive breakpoint patterns used in `apps/web/src/routes/privacy.tsx` (the `px-6 lg:px-4` container padding convention, `sm:` typography scaling, `lg:` layout adjustments).
- Do not introduce any new npm dependencies or UI components not already used by this file.

KNOWN-UNKNOWNS
- The user says "make the page responsive" but the file already has responsive Tailwind classes (`px-6`, `lg:px-4`, `sm:pt-24`, `sm:text-header-xl`). It is unclear whether the user sees a specific rendering bug on certain viewports, or whether they want the spacing/typography tuned further to match `privacy.tsx` more closely (e.g., `space-y-4` vs `space-y-5`, adding `text-lg` to the lead paragraph).
- The content body currently uses fixed `text-2xl` for the `<h3>` heading and no `text-lg` lead paragraph, unlike `privacy.tsx`. It is unclear whether the heading and text should be scaled at smaller breakpoints.
- Whether the `overflow-x-hidden` on `<main>` is intentional as a global safety net or was added to work around a specific layout issue that should instead be fixed at the source.

EXTERNAL-DEPENDENCIES
- TanStack React Router  — file-based routing for the accessibility page route definition
- TanStack Start (Nitro)  — SSR framework powering the `apps/web` server
- `@pmigov/ui` components (Breadcrumb) — shared UI component library consumed via workspace path

## verified tooling

ultracite check
bun run check-types
bun run build
bun run dev
bun run docker:up

## research

FILES
apps/web/src/routes/accessibility.tsx  target file — the page to make responsive
apps/web/src/routes/privacy.tsx  reference file — responsive patterns to match
packages/ui/src/styles/globals.css  design tokens (CSS custom properties, font sizes, PMI color/spacing variables)
packages/ui/src/components/breadcrumb.tsx  Breadcrumb component API — used by both pages, read-only
apps/web/src/routes/refund-policy.tsx  sibling page confirming the same responsive pattern (space-y-5, font-aeonik, text-lg lead, h2 headings)
apps/web/src/routes/advertising.tsx  sibling page confirming the same responsive pattern

APIS
Here is the complete research output:

  createFileRoute  TanStack Router file-based route definition, used as `createFileRoute("/accessibility")`
  Link  TanStack Router component, passed as `render={<Link to="/" />}` to BreadcrumbLink
  Breadcrumb  Nav wrapper from `@pmigov/ui/components/breadcrumb`, renders `<nav aria-label="breadcrumb">`
  BreadcrumbList  Ordered list wrapper, applies `flex flex-wrap items-center gap-1.5 text-sm`
  BreadcrumbItem  List item wrapper, applies `inline-flex items-center gap-1.5`
  BreadcrumbLink  Anchor with render-prop pattern via `useRender`, accepts `render={<Link to="/" />}`
  BreadcrumbPage  Current-page span, applies `font-normal text-foreground`
  BreadcrumbSeparator  Chevron separator, defaults to `<IconChevronRight />`

  --text-header-md  CSS custom property, `2.25rem` (36px) — mobile hero heading size, applied via `text-header-md`
  --text-header-xl  CSS custom property, `3.5rem` (56px) — sm+ hero heading size, applied via `sm:text-header-xl`
  --pmi-surface-primary  CSS custom property, `rgb(255,255,255)` — white, applied via `bg-pmi-surface-primary`
  --pmi-surface-secondary  CSS custom property, `#f8f5f0` — warm off-white, applied via `bg-pmi-surface-secondary`
  --pmi-text-primary  CSS custom property, `rgb(32,15,59)` — dark purple, applied via `text-pmi-text-primary`
  --pmi-rounded-md  CSS custom property, `24px` — content card border-radius, referenced in inline `style`
  --font-aeonik  CSS custom property, `"DM Sans Variable"` — applied via `font-aeonik` utility

  tailwindcss  CSS utility classes used for all responsive styling — `px-6`, `lg:px-4`, `sm:pt-24`, `sm:pb-20`, `text-header-md`, `sm:text-header-xl`, `text-2xl`, `text-lg`, `leading-7`, `space-y-4`, `space-y-5`
  tailwindcss responsive prefixes  `sm:` (640px), `lg:` (1024px) — the breakpoints used in the hero/content card pattern across all sibling content pages

  createFileRoute("/accessibility")  Route definition — must be preserved exactly
  RouteComponent  The sole component exported by the route — sole target of all edits

DIFFERENCES TO FIX (accessibility.tsx vs established pattern in privacy.tsx, refund-policy.tsx, advertising.tsx, purchasing-terms.tsx):

  content wrapper div classes  Current: `space-y-4 text-pmi-text-primary` → Target: `space-y-5 font-aeonik text-pmi-text-primary` (matches all 4 sibling pages)
  h3 heading classes  Current: `font-aeonik font-semibold text-2xl` → Target: `font-semibold text-lg` (redundant `font-aeonik` removed since on wrapper; `text-2xl`→`text-lg` matches privacy.tsx:94, refund-policy.tsx:257)
  first paragraph (lead)  Current: `font-aeonik leading-7` → Target: `text-lg leading-7` (add `text-lg` for lead emphasis, matches privacy.tsx:51)
  all body paragraphs  Current: each has `font-aeonik leading-7` → Target: `leading-7` only (font-aeonik inherited from wrapper, matches privacy.tsx:60+)
  subheading paragraphs  Current: `<p className="font-aeonik font-semibold">` → Target: `<h3 className="font-semibold text-lg">` (semantic upgrade + size matches project convention; no text content change)

HERO SECTION PATTERN (already correct in accessibility.tsx, no changes needed):
  breadcrumb container  `<div className="mx-auto max-w-screen-xl px-6 pt-6 lg:px-4">`
  heading container  `<div className="mx-auto max-w-screen-xl px-6 pt-20 pb-16 sm:pt-24 sm:pb-20 lg:px-4">`
  h1 heading  `<h1 className="mb-6 font-aeonik font-bold text-header-md text-pmi-text-primary sm:text-header-xl">`

CONTENT CARD PATTERN (already correct in accessibility.tsx, no changes needed):
  card outer  `<div className="relative w-full overflow-hidden bg-pmi-surface-primary" style={{ borderRadius: "var(--pmi-rounded-md) var(--pmi-rounded-md) 0 0", marginTop: "calc(-1 * var(--pmi-rounded-md))" }}>`
  card inner  `<div className="mx-auto max-w-screen-xl px-6 py-6 lg:px-4 lg:py-10">`

CONSTRAINTS TO VERIFY:
  main element  `<main className="overflow-x-hidden" id="main-layout">` — preserved as-is
  internal href values  `/about/contact` and `/certifications/certification-resources/accommodations` — preserved verbatim
  link styling  `className="underline underline-offset-2 hover:opacity-70"` — preserved on both links
  no new imports  File uses only `createFileRoute`, `Link`, and 6 Breadcrumb components — no additions
  no new dependencies  All Tailwind utilities and design tokens already available in the project

CONTEXT
The page already has the same structural template as `privacy.tsx` and all other sibling legal/info pages — the hero layout, breadcrumb positioning, content card with `--pmi-rounded-md` overlap, and `overflow-x-hidden` on `<main>` are all identical. The responsive issue is limited to content-card typography and spacing differences, not structural layout.

Here is the raw context section:

- Accessibility page (`apps/web/src/routes/accessibility.tsx`) and privacy page (`apps/web/src/routes/privacy.tsx`) share an identical structural template: `<main className="overflow-x-hidden">` → hero with `bg-pmi-surface-secondary` → breadcrumb in `max-w-screen-xl px-6 pt-6 lg:px-4` → heading in `max-w-screen-xl px-6 pt-20 pb-16 sm:pt-24 sm:pb-20 lg:px-4` with `text-header-md sm:text-header-xl` → content card with `bg-pmi-surface-primary`, inline `style={{ borderRadius: "var(--pmi-rounded-md) var(--pmi-rounded-md) 0 0", marginTop: "calc(-1 * var(--pmi-rounded-md))" }}` → content wrapper `max-w-screen-xl px-6 py-6 lg:px-4 lg:py-10`

- The only content-card differences between accessibility.tsx and privacy.tsx are: (a) accessibility uses `space-y-4` on the content wrapper while privacy uses `space-y-5`; (b) accessibility lacks `font-aeonik` on the content wrapper div (privacy applies it there); (c) accessibility's `<h3>` uses `text-2xl` while privacy's `<h3>` uses `text-lg` and `<h2>` uses `text-xl`; (d) accessibility lacks `text-lg` on the lead paragraph while privacy applies `text-lg leading-7` to its first `<p>`

- All six sibling legal pages (privacy, terms-of-use, advertising, refund-policy, purchasing-terms, accessibility) share the same `main` wrapper, hero section, breadcrumb placement, and content card styling — they differ only in content and minor typography classes

- The accessibility page uses `space-y-4` on its content div, which is tighter than `space-y-5` used by privacy.tsx, refund-policy.tsx, terms-of-use.tsx, advertising.tsx, and purchasing-terms.tsx (all of which use `space-y-5`)

- Design tokens are defined in `packages/ui/src/styles/globals.css`: `--pmi-surface-primary: rgb(255,255,255)`, `--pmi-surface-secondary: #f8f5f0`, `--pmi-text-primary: rgb(32,15,59)`, `--pmi-rounded-md: 24px`, `--text-header-xl: 3.5rem` (56px), `--text-header-md: 2.25rem` (36px), `--font-aeonik: "DM Sans Variable", "DM Sans", Arial, sans-serif`

- The root layout in `apps/web/src/routes/__root.tsx` uses `grid min-h-svh grid-rows-[auto_1fr_auto]` with `<Header />`, `<Outlet />`, and `<Footer />` — the header includes an announcement banner (48px), a top utility bar (56px), and a primary nav bar (48px), totaling ~152px before the page content

- The viewport meta tag is already set to `width=device-width, initial-scale=1` in `__root.tsx`

- Tailwind v4 is in use (`@import "tailwindcss"` in globals.css), with `sm:` at 640px, `md:` at 768px, `lg:` at 1024px, `xl:` at 1280px

- The `<main>` element has both `overflow-x-hidden` (intentional horizontal scroll prevention) and `id="main-layout"` — this is consistent across all sibling pages and is not a workaround

- All anchor `<a>` tags in accessibility.tsx use native HTML `<a>` with `href` (not TanStack `<Link>`), with classes `underline underline-offset-2 hover:opacity-70` — matching the pattern in privacy.tsx and other sibling pages

- The Breadcrumb component is imported from `@pmigov/ui/components/breadcrumb` and is used identically across all sibling pages with no prop differences

VERIFIED-TOOLING
  ultracite check
  bun run check-types
  bun run build
  bun run dev
  bun run docker:up

## grill Q&A

(no questions produced)

## spec

GOAL
Make the page at `apps/web/src/routes/accessibility.tsx` fully responsive across all viewport widths by aligning its content-card typography and spacing classes with the established pattern used by `privacy.tsx`, `refund-policy.tsx`, `advertising.tsx`, and `purchasing-terms.tsx`. The changes are limited to class attribute edits on existing elements: update the content wrapper's spacing and font inheritance, downsize `<h3>` headings from `text-2xl` to `text-lg`, add `text-lg` to the lead paragraph, remove redundant per-element `font-aeonik` declarations (inherited from the wrapper), and upgrade subheading `<p>` elements to `<h3>` for semantic correctness matching the project convention. No structural, layout, or content changes are needed.

CONSTRAINTS
- Preserve every existing element, component import, route definition (`createFileRoute("/accessibility")`), and semantic structure (`<main>`, hero `<div>`, content card `<div>`) exactly as they are.
- Preserve all existing internal link `href` values (`/about/contact`, `/certifications/certification-resources/accommodations`) verbatim.
- Preserve the existing design token classes (`bg-pmi-surface-secondary`, `bg-pmi-surface-primary`, `text-pmi-text-primary`, `font-aeonik`, `--pmi-rounded-md`) — do not replace them with hardcoded colors or values.
- Preserve the inline `style` object on the content card (`borderRadius`, `marginTop` using CSS custom properties).
- Preserve `<main className="overflow-x-hidden" id="main-layout">` exactly as-is — this is intentional and consistent across all sibling pages.
- Do not add or remove any textual content, headings, paragraphs, or links.
- Do not change the Breadcrumb component or its props.
- Do not change the hero section classes — they are already correct.
- Do not change the content card outer/inner wrapper classes — they are already correct.
- Do not introduce any new npm dependencies or UI components not already used by this file.
- Match the responsive breakpoint patterns already present — no new breakpoints needed since the structural layout is identical to `privacy.tsx`.

ACCEPTANCE
- The content wrapper `<div>` inside the content card uses `space-y-5 font-aeonik text-pmi-text-primary` (changed from `space-y-4 text-pmi-text-primary`).
- Every `<h3>` heading inside the content card uses `font-semibold text-lg` (changed from `font-aeonik font-semibold text-2xl`) — the `font-aeonik` class is removed because it is inherited from the wrapper.
- The first `<p>` (lead paragraph) after the first `<h3>` uses `text-lg leading-7` (changed from `font-aeonik leading-7`) — `font-aeonik` removed (inherited), `text-lg` added for lead emphasis.
- All subsequent body `<p>` elements use only `leading-7` (changed from `font-aeonik leading-7`) — `font-aeonik` removed (inherited).
- Subheading `<p className="font-aeonik font-semibold">` elements are converted to `<h3 className="font-semibold text-lg">` — semantic upgrade with matching size.
- All internal `href` values are preserved verbatim.
- The page renders without horizontal scrolling on viewports from 320px through 1440px+.
- The page builds and type-checks without errors.

VERIFY:
```sh
cd /home/yash/projects/pmigov && bun run check-types && bun run build && grep -n 'space-y-\|font-aeonik\|text-2xl\|text-lg\|leading-7\|<h3\|<p ' apps/web/src/routes/accessibility.tsx
```

## phase timings

refine               46.4s
research            313.2s
  enrichment              2.3s
  worker:files wait      762ms
  worker:files work      57.7s
  worker:apis wait       830ms
  worker:apis work      103.6s
  worker:context wait    771ms
  worker:context work    59.0s
  worker:tooling wait    746ms
  worker:tooling work    36.2s
  workers               261.8s
  verify-tooling         51.4s
grill                 6.1s
  gen                     6.1s
compose              15.4s
critique            113.8s
  triage                 55.5s
  rewrite                58.3s
total               494.9s

## handoff

handoff_at: 2026-06-30T16:34:12.812Z
